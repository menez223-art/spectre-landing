# CLAUDE.md — قواعد العمل على صفحات الهبوط (Landing Pages)

هذه القواعد إلزامية لكل مشروع صفحة هبوط جديد. راجعها قبل بدء أي عمل.

## 1. الهيكل الثابت لصفحات الهبوط

كل صفحة هبوط يجب أن تتبع نفس الهيكل التالي وبالترتيب نفسه:

1. **Header** — شريط علوي يحتوي على الشعار وروابط التنقل الأساسية.
2. **Product Showcase** — عرض المنتج الرئيسي (صورة/فيديو + وصف + دعوة للإجراء CTA).
3. **Features** — قسم المزايا الرئيسية للمنتج.
4. **Testimonials** — آراء العملاء / الشهادات.
5. **Express Form** — نموذج طلب سريع لجمع بيانات العملاء المحتملين.

> لا يُسمح بإضافة أو إزالة أقسام من هذا الهيكل دون طلب صريح من المستخدم.

## 2. الألوان الديناميكية (Color Palette)

- الألوان **ليست ثابتة** — تتغير مع كل منتج/مشروع.
- عند بدء مشروع جديد:
  - استخرج **لوحة ألوان (Color Palette)** من **صورة المنتج** نفسه.
  - اجعل الألوان الناتجة **متناسقة** مع هوية المنتج (خلفيات، نصوص، أزرار، حالات hover).
  - حدّد أدوار الألوان بوضوح: أساسي (Primary)، ثانوي (Secondary)، خلفيات، نصوص، ألوان حالات (نجاح/تنبيه/خطأ).
- الهدف: كل صفحة هبوط تُقرأ كأنها صُمّمت خصيصًا لهذا المنتج، وليست مجرد قالب مُلوّن عشوائيًا.

## 3. معايير السرعة والأداء والتصميم

- الالتزام التام بمعايير مهارتي:
  - **`/frontend-design`** — لجودة التصميم والممارسات الصحيحة لواجهات المستخدم.
  - **`/vercel-react-best-practices`** — لأفضل ممارسات React عند النشر على Vercel.
- تطبيق هذه المهارات عند كل مرحلة من مراحل البناء:
  - التخطيط، التصميم، كتابة الكود، والمراجعة النهائية.
- الأداء والسرعة (Performance & Speed) معيار غير قابل للتفاوض: تحسين التحميل، الصور، التقسيم (code-splitting)، والتخزين المؤقت.

## 4. قاعدة حماية الإنتاج (إلزامية ودائمة — بأمر صريح من المالك)

> **«لا تضف شيئاً للمشروع الذي في الإنتاج»** — أمر قاطع. المشروع المنشور على
> **النطاق الإنتاجي الصحيح: `https://spectre-dz.vercel.app/`**
> (`spectre-tau-five.vercel.app` نطاق **ميت** — يُرجع 404. صُحِّح في 2026-09-09.)
> هذا المشروع **خط أحمر**: لا يُلمَس بأي شكل.

**الممنوعات المطلقة (بدون استثناء، إلا بأمر صريح في نفس الجلسة):**

1. ❌ **النشر** — لا `vercel deploy`، لا نشر تلقائي، لا نشر «ضمن خطوة أخرى».
2. ❌ **الالتزام (commit)** — لا `git commit` بدون أمر صريح.
3. ❌ **تعديل متغيرات الإنتاج** — لا إضافة/تغيير/حذف أي متغير بيئة على Vercel.
4. ❌ **سحب أسرار الإنتاج** — لا `vercel env pull` ولا نداء API لجلب قيم الإنتاج،
   حتى لو كان الغرض «القراءة فقط». (جُرِّب في 2026-09-08: `vercel env pull` نجح
   تقنياً لكن القيم كُتبت مُقنَّعة، ونداء API ردّ `403 forbidden — re-authenticate
   to scope "menez223-7187s-projects"`. **النتيجة: لا فائدة ومخاطرة بلا مقابل.**)
5. ❌ **الكتابة على قاعدة الإنتاج** — لا `setKv`/حذف/حظر من بيئة محلية متصلة بـ Supabase.

**المسموح:** العمل داخل مجلد العمل المحلي فقط (قراءة، تعديل ملفات، بناء، اختبار)،
والتوثيق في `docs/` و `memory/`. أي شيء يخرج من الجهاز يحتاج موافقة صريحة.

- مُعتمد في: `docs/CHECKPOINT.md` §ل10/§ل11/§ل13 + ذاكرة المشروع `.workbuddy-ai/memory/`.

## 5. بيئة التطوير المحلية (حقائق مؤكدة)

- `.env.local` **يفتقر إلى `SUPABASE_SERVICE_ROLE_KEY`** → `hasSupabase()` = false محلياً،
  فتُقرأ البيانات من مخزن التطوير `.dev-kv/kv.json` (فارغ من `subs/`). أي «لم أجد المشتركين»
  محلياً سببه هذا، لا خلل في الكود.
- `RESEND_API_KEY` **صالح** لكنه مفتاح **إرسال فقط** (`GET /domains` → 401 restricted،
  وهذا طبيعي). لا تستنتج «المفتاح غير صالح» من ردّ 401 على `/domains` — ولا تنسَ
  تجريد علامتَي التنصيص عند اختبار أي قيمة من ملف `.env`.
- `scripts/env-health.mjs` (جديد) يفحص كل ذلك بلا طباعة أسرار: `node scripts/env-health.mjs`.
- `npx next build` يحتاج `CODEBUDDY_SAFE_DELETE_ENABLED=0` في هذه البيئة (حارس الحذف
  يوقفه عند 50 حذفاً داخل `.next`) — الفشل بيئي لا كودي.
